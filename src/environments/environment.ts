// The file contents for the current typography will overwrite these during build.
// The build system defaults to the dev typography which uses `typography.ts`, but if you do
// `ng build --env=prod` then `typography.prod.ts` will be used instead.
// The list of which env maps to which file can be found in `.angular-cli.json`.

export const typography = {
  production: false
};
